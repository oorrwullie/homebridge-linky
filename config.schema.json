{
  "pluginAlias": "Linky",
  "pluginType": "platform",
  "schema": {
    "type": "object",
    "required": ["platform"],
    "properties": {
      "platform": {
        "type": "string",
        "const": "Linky",
        "description": "The name of the platform."
      },
      "port": {
        "type": "number",
        "title": "API Server Port",
        "default": 8081,
        "description": "The local port the Linky API will run on."
      },
      "apiKey": {
        "type": "string",
        "title": "API Key",
        "description": "Used to authenticate API requests. If empty, one is generated automatically and saved."
      },
      "uiPort": {
        "type": "number",
        "title": "Homebridge UI Port",
        "default": 8581,
        "description": "Used to talk to the Homebridge config editor API."
      }
    }
  }
}
